load("@rules_cc//cc:cc_binary.bzl", "cc_binary")
cc_library(
	name = "app_lib",
	srcs = glob(["**/*.cc"]),
	hdrs = glob(["**/*.h"], allow_empty = True),
)
cc_binary(
	name = "app",
	deps = [":app_lib"],
	visibility = ["//visibility:public"],
)
